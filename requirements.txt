# Core FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Essential LangChain components only - REDUCED SCOPE
langchain-core==0.1.52
langchain-community==0.0.38
langchain-text-splitters==0.0.1
langchain-huggingface==0.0.3

# Vector database - minimal
chromadb==0.4.18

# Document processing - MINIMAL SET
unstructured==0.11.6
python-multipart==0.0.6

# Core ML - CPU only, SPECIFIC VERSIONS for memory optimization
sentence-transformers==2.2.2
transformers==4.35.2
torch==2.1.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu
numpy==1.24.4

# Essential utilities only
pydantic==2.5.0
aiofiles==23.2.0

# Document parsing - MINIMAL SET
pypdf==3.17.1
openpyxl==3.1.2

# REMOVED: Heavy dependencies
# - pandas (save ~50MB)
# - ollama (save memory)
# - python-docx (save space)
# - xlrd (use openpyxl only)
# - unstructured[pdf] (use base version)
# - python-dotenv (not needed)
